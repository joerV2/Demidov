#include "graphics.h"
#include "picture.hpp"
void man() 
{
   setcolor(CYAN);

    // Рисуем голову
    circle(400, 250, 25);

    // Рисуем туловище
    line(400, 275, 400, 375);

    // Рисуем руки
    line(400, 300, 350, 350);  // Левая рука
    line(400, 300, 450, 350);  // Правая рука

    // Рисуем ноги
    line(400, 375, 350, 450);  // Левая нога
    line(400, 375, 450, 450);  // Правая нога

    // Добавляем глаза для выразительности
    circle(390, 245, 3);
    circle(410, 245, 3);

}